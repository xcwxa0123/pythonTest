KAKUYOMU_PARAMS = {
    'TARGET_DOM_CLASS': 'js-episode-body', # 目标TEXT所在DOM的class
    'WRITE_DIR_NAME': 'kakuyomu_transed', # 要写入的文件夹（没有会创建）
    'TARGET_CREATE_PATH': './', # WRITE_DIR_NAME所在地址
    'TEXTFILE_PATH': 'test' # 扒下来的HTML文件所在文件夹
}